// Test stubs generated during planning for inbox-concept/phase-2/task-03-auth-routing
// Implement these during task execution — do not delete pending stubs

describe('api-routes — inbox middleware', () => {
  // Story 3 / Scenario 1
  it.todo('resolves req.inbox when ?inbox=<valid inboxToken> is provided')

  // Story 3 / Scenario 2
  it.todo('attaches the first active messenger inbox when no ?inbox param is present')

  // Story 3 / Scenario 3
  it.todo('still resolves licensee correctly when only ?department=<token> is provided (backward compat)')

  // Story 3 / Scenario 4
  it.todo('returns 401 when ?inbox=<unknown token> is provided')

  it.todo('proceeds without inbox when licensee has no inboxes at all')
})
