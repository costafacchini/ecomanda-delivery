PORT=5000
NODE_ENV=development
DEFAULT_USER=user@test.com
DEFAULT_PASSWORD=12345678
SECRET=
REDIS_URL=redis://127.0.0.1:6379
MONGODB_URI=mongodb://root:pwk372ew@localhost/ecomanda-delivery
MONGOMS_DEBUG=0
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
STORAGE_PROVIDER=local       # local | s3 (default: s3)
LOCAL_STORAGE_PATH=/app/uploads  # only used when STORAGE_PROVIDER=local
APP_URL=http://localhost:5000    # used by LocalStorage to build public file URLs
SKIP_PREFLIGHT_CHECK=true #HEROKU ONLY

# PostgreSQL (required from Phase 2 of mongo-to-postgres migration onwards)
# Heroku: provisioned automatically via `heroku addons:create heroku-postgresql:essential-0`
# Local dev: see postgres service in docker-compose.yml
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ecomanda_delivery?schema=public"

# --- Docker Compose (local dev) ---
# Copy this file to .env and fill in the secrets below.
# docker-compose reads .env automatically.
SECRET=change-me
DEFAULT_USER=admin@example.com
DEFAULT_PASSWORD=changeme123
